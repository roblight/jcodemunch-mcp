{
  "name": "jcodemunch-mcp-vscode",
  "displayName": "jCodeMunch — Auto Reindex + Risk Gutter",
  "description": "Auto-reindexes saved files and paints a per-symbol risk-density gutter (yellow/orange/red dots at risky function headers, hover for breakdown). Powered by jcodemunch-mcp.",
  "version": "0.2.0",
  "publisher": "jgravelle",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/roblight/jcodemunch-mcp",
    "directory": "vscode-extension"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "jCodeMunch",
      "properties": {
        "jcodemunch.indexOnSave.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Auto-run `jcodemunch-mcp index-file <path>` on file save."
        },
        "jcodemunch.indexOnSave.command": {
          "type": "string",
          "default": "jcodemunch-mcp",
          "description": "Path to the jcodemunch-mcp executable (defaults to looking on PATH)."
        },
        "jcodemunch.indexOnSave.debounceMs": {
          "type": "number",
          "default": 500,
          "description": "Per-file debounce window in milliseconds — rapid successive saves of the same file collapse into a single reindex."
        },
        "jcodemunch.indexOnSave.exclude": {
          "type": "array",
          "items": { "type": "string" },
          "default": [
            "**/node_modules/**",
            "**/.git/**",
            "**/dist/**",
            "**/build/**",
            "**/.venv/**",
            "**/venv/**",
            "**/__pycache__/**",
            "**/*.min.*"
          ],
          "description": "Glob patterns for files to skip on save."
        },
        "jcodemunch.riskGutter.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Paint a per-symbol risk-density gutter (yellow/orange/red dots at function headers; green = invisible). Refreshes on file open + on save. Disable if it gets noisy."
        },
        "jcodemunch.riskGutter.debounceMs": {
          "type": "number",
          "default": 600,
          "description": "Per-file debounce window for the risk-gutter refresh on save."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
